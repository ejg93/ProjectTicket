# 게이트: gitleaks
# 명령: mkdir -p probe-leak && printf 'token = "ghp_%s"\n' "$(LC_ALL=C tr -dc 'A-Za-z0-9' </dev/urandom | head -c 36)" > probe-leak/leak.txt && MSYS_NO_PATHCONV=1 docker run --rm -v "$(pwd -W 2>/dev/null || pwd)/probe-leak:/scan" zricethezav/gitleaks:v8.18.4 detect --source /scan --no-git --no-banner
# 문구: leaks found
