# 게이트: MigrationTextTest 옥텟 길이
# 명령: cd backend && ./gradlew test --tests '*MigrationTextTest' --no-daemon --build-cache -q; r=$?; cat build/test-results/test/*MigrationTextTest.xml 2>/dev/null; exit $r
# 문구: 글자 수(`length`)로 잰다
diff --git a/backend/src/main/resources/db/migration/V900__probe_length.sql b/backend/src/main/resources/db/migration/V900__probe_length.sql
new file mode 100644
index 0000000..4f6cabf
--- /dev/null
+++ b/backend/src/main/resources/db/migration/V900__probe_length.sql
@@ -0,0 +1,2 @@
+-- probe
+alter table account add constraint account_probe_check check (length(email) <= 254);
