# 게이트: ArchitectureTest PUT·PATCH 금지
# 명령: cd backend && ./gradlew test --tests '*ArchitectureTest' --no-daemon --build-cache -q; r=$?; cat build/test-results/test/*ArchitectureTest.xml 2>/dev/null; exit $r
# 문구: should be annotated with @PatchMapping
diff --git a/backend/src/main/kotlin/com/projectticket/ticket/health/ProbePatchController.kt b/backend/src/main/kotlin/com/projectticket/ticket/health/ProbePatchController.kt
new file mode 100644
index 0000000..f20ae95
--- /dev/null
+++ b/backend/src/main/kotlin/com/projectticket/ticket/health/ProbePatchController.kt
@@ -0,0 +1,10 @@
+package com.projectticket.ticket.health
+
+import org.springframework.web.bind.annotation.PatchMapping
+import org.springframework.web.bind.annotation.RestController
+
+@RestController
+class ProbePatchController {
+    @PatchMapping("/api/probe")
+    fun probe(): String = "probe"
+}
