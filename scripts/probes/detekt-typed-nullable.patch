# 게이트: detekt 타입 해석 규칙 전부(check → detektMain, G8). `check` 로 부른다 — 태스크를 바로 부르면 `check` 에서 빠져도 막는다(마무리 15차)
# 명령: cd backend && ./gradlew check -x test -x integrationTest --no-daemon --build-cache -q
# 문구: UnsafeCallOnNullableType
diff --git a/backend/src/main/kotlin/com/projectticket/ticket/payment/ProbeNullable.kt b/backend/src/main/kotlin/com/projectticket/ticket/payment/ProbeNullable.kt
new file mode 100644
index 0000000..6fbac8e
--- /dev/null
+++ b/backend/src/main/kotlin/com/projectticket/ticket/payment/ProbeNullable.kt
@@ -0,0 +1,4 @@
+package com.projectticket.ticket.payment
+
+/** 탐침 — 근거 없는 `!!` 가 `detektMain` 을 세우나(`G8`) */
+internal fun probeNullable(value: String?): Int = value!!.length
