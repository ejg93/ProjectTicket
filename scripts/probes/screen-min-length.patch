# 게이트: ScreenLengthTest 하한(`G9-1`)
# 명령: cd backend && ./gradlew test --tests '*ScreenLengthTest' --no-daemon --build-cache -q; r=$?; cat build/test-results/test/*ScreenLengthTest.xml 2>/dev/null; exit $r
# 문구: 칸은 `minLength={12}`
diff --git a/frontend/src/app/signup/signup-form.tsx b/frontend/src/app/signup/signup-form.tsx
--- a/frontend/src/app/signup/signup-form.tsx
+++ b/frontend/src/app/signup/signup-form.tsx
@@ -79,7 +79,7 @@ export function SignupForm({ items }: { items: ConsentItem[] }) {
     <form action={submit}>
       <Field name="email" label="이메일" type="email" autoComplete="email" maxLength={254} defaultValue={draft?.email} />
       <Field name="display_name" label="이름" autoComplete="name" maxLength={50} defaultValue={draft?.displayName} />
-      <Field name="password" label="비밀번호" type="password" autoComplete="new-password" maxLength={64} minLength={15} />
+      <Field name="password" label="비밀번호" type="password" autoComplete="new-password" maxLength={64} minLength={12} />
       <p className="muted">비밀번호는 15자 이상으로 정해 주세요.</p>
 
       <fieldset>
