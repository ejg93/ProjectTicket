# ProjectTicket

인터파크식 공연 예매 시스템. 목적은 **예매 백엔드·아키텍처를 익히는 것**과 포트폴리오다.
주간 토큰이 남을 때 청크 하나씩 실행한다. 세션이 끊기는 것을 전제로, 진행 상태는 전부 파일에 있다.

**이 저장소에서는 이 예매 프로젝트 작업만 다룬다.** `C:\workspace\ProjectShop` 은 이식 원본이라 **읽기만 한다** — 그쪽 파일을 안 고친다.

## 어디를 읽나

| 들은 말 | 읽는 것 |
|---|---|
| 세션에서 처음 받은 말 | **「예열」이 먼저다**(`/warmup`). 안 하고 청크로 안 간다 |
| `다음 청크 해` | 「재개 프로토콜」 → 「청크 하나를 어떻게 치나」 |
| `/design`(또는 `설계`) | 그 스킬. 번들 앞에 한 번. 갈림을 묻고 행을 적는다. Fable 몫 |
| `번들 해` | 「재개 프로토콜」의 번들 모드 — 설계된 행을 순서대로 |
| `/verify`(또는 `검증`) | 그 스킬. 명령 표가 거기 있다 |
| `/wrapup`(또는 `마무리`) | 그 스킬. 청크 여럿 친 뒤 한다 |
| `/overnight`(또는 `밤샘`) | 그 스킬. 번들 여럿을 사용자 없이 머지까지 — 자기 전에 결정을 한 번에 받는다 |
| `/inspection`(또는 `점검`) | 그 스킬. 저장소 전체를 한 축으로 훑는다 |
| 판단이 갈릴 때 | 「대전제」 |
| 코드를 쓸 때 | 「코드를 쓸 때 항상 지키는 것」 |

**「글 작성 규칙」·「토큰 절약」은 글을 쓰고 파일을 읽는 모든 순간에 걸린다.**

## 대전제

1. **공부이면서 실물이다.** 예매 시스템이 실제로 가진 확장 형태(다중 인스턴스·대기열·이벤트 분리)로 만든다. 돌아가기만 하는 코드는 목적의 절반이다.
2. **계층을 건드리는 결정을 먼저 한다.** 무엇을 어느 층에 둘지(타입·스키마·DB 제약·앱 검증·문서)를 코드보다 먼저 정한다 — `coding-rules.md` 「축 2 — 강제 지점」.
3. **부르는 곳을 세고 만든다.** 소비자가 하나면 추상화·브로커·서비스 분리를 안 한다. Kafka 가 그 예다 — 둘째 소비자가 생기는 청크에서 들인다(ADR 0001).
4. **핫패스는 동기다.** 좌석 선점·확정은 사용자가 즉시 답을 받아야 한다. 비동기는 확정 **뒤**에만 둔다.
5. **ProjectShop 보다 문서 비중을 낮춘다.** 그쪽은 커밋의 절반이 문서였다. 여기서는 **코드 청크 셋에 문서 청크 하나**를 넘지 않는다. 기록은 짧게, 강제 지점은 코드로.

## 예열

`/warmup` 이 세션을 여는 것이다. 코드를 안 건드리고 규칙 뼈대를 훑고 오늘 걸리는 축을 말한다.
**세션마다 한 번.** 사용자가 바로 청크를 시켜도 예열이 먼저다.

## 재개 프로토콜

`다음 청크 해` 를 들으면 이 순서다. 계획 전체를 다시 설명하지 않는다.

1. 「현재 상태」가 이미 컨텍스트에 있다(SessionStart hook). 그 블록이 단일 진실이다
2. `진행중` 청크가 있으면 그것부터
3. 없으면 `PLAN.md` 분할표에서 선행이 `완료` 인 다음 청크. **무엇부터**는 아래 표
4. `PLAN.md` 는 **그 청크에 걸린 행만** 읽는다. 통짜로 읽지 않는다
5. 「청크 하나를 어떻게 치나」대로 친다
6. **커밋한다. 청크 하나 = 커밋 하나.** `main` 에 직접 안 민다 — 훅이 막는다. `work/<날짜>` 가지에 쌓고 마무리가 PR 을 연다

사용자가 남은 토큰 비율을 알려주면 그에 맞춰 청크 개수를 조절한다. 안 알려주면 청크 하나만 하고 멈춘다.

### 번들 모드 — `번들 해`

설계 세션(`/design`)이 적어 둔 행을 순서대로 친다. 청크마다 1~6 은 같다. 다른 것:

- 「시작 세 줄」은 행에 이미 있다. 말하지 않고 확인만 한다
- **갈려도 안 멈춘다** — 실패 사다리, 없으면 정하고 이력에 「정한 것(계획 밖)」. 행당 둘까지, 넘으면 WIP 로 빼고 다음 행
- 청크 verify 가 빨가면 고치기 둘까지. 그래도면 WIP 로 빼고 다음 행
- **WIP 는 번들 가지에 안 올린다** — `wip/<청크>` 곁가지에 커밋하고 번들 가지로 돌아온다. 빨간 트리가 뒤 청크의 verify 를 다 빨갛게 하기 때문이다. commit hook 이 `work/*` 에서 도장 없는 커밋을 막는다
- Docker 가 꺼져 있어도 안 선다 — `verify.sh` 가 먼저 켜고(`scripts/docker-up.sh`, 3분), 그래도 안 뜨면 빠른 레인만 돌리고 도장을 `fast-nodb` 로 남긴다. 번들 끝 `--full` 이 잡는다
- 절반 지점에서 시간·토큰을 한 번 보고 나머지를 갈지 말한다
- 사용자가 자는 동안 번들 여럿을 머지까지 치는 것은 `/overnight` 가 앞뒤를 든다
- 다 치면 `/wrapup` — 끝 보고가 계획 밖 결정·WIP·신규 행을 모아 낸다

### 무엇부터

| 순위 | 무엇 | 왜 |
|---|---|---|
| 1 | 지금 깨져 있는 것(빨간 테스트·CI) | 다른 검증의 신호를 가린다 |
| 2 | 쓰는 곳이 늘면 비싸지는 것(응답 계약·스키마) | 소비자 수에 비례해 값이 오른다 |
| 3 | 결정이 막고 있는 것 | 먼저 물어 두면 나머지가 안 멈춘다 |
| 4 | 나머지 | 지금이나 나중이나 값이 같다 |

### 문서가 코드보다 먼저다 — 조건이 찼을 때만

기준 문서(`PLAN.md` 「기준 문서」의 D 줄) 중 **선행이 완료이고 정할 근거가 코드에 하나라도 생긴 것**은 코드 청크보다 먼저 쓴다.
근거가 없는데 쓰면 무엇을 정하는지 모른 채 정한다. 성능 목표(`D13`)가 그 예다 — 측정값 없이는 안 쓴다.

## 청크 하나를 어떻게 치나

### 시작 — 세 줄을 말하고 들어간다

| 무엇 | 예 |
|---|---|
| **무엇을 닫나** | 「`10` — 같은 좌석을 둘이 잡을 수 있다」 |
| **걸리는 축** | 「표준(SQL 격리 수준) + DB 제약 + 동시성 규약(`D4`)」 |
| **강제 지점을 어디에 두나** | 「제약으로 내린다. 못 내리면 테스트, 그것도 안 되면 왜 못 내리는지 적는다」 |

셋째가 핵심이다. 「다음에 조심한다」로 끝나면 그 청크는 아무것도 안 막은 것이다.
분할표의 미착수 행이 넷(축·강제 지점·건드리는 자리·닫힘)을 미리 적어 뒀다. 읽고 들어가되 **치면서 달라지면 그 자리에서 밝힌다.**

### 중간 — 갈리면 멈춘다

- 결정이 갈리면 사용자에게 선택지를 내민다. 기술 판단이라도 임의로 정하고 넘어가지 않는다
- 규약과 다르게 짜야 하면 **문서를 먼저 고친다.** 코드가 먼저 가면 다음 사람이 그것을 관례로 본다
- 문서는 그 청크가 실제로 건드리는 것만 편다

### 끝 — 이 다섯을 채워야 닫힌다

| 무엇 | 안 하면 |
|---|---|
| `/verify` 를 돌렸다 | `bash scripts/verify.sh` 가 레인을 고르고 도장을 찍고 Stop hook 이 도장을 본다. **못 돌렸으면 못 돌렸다고 밝힌다** |
| 남긴 강제 지점을 한 줄로 적는다 | 제약·트리거·테스트 중 무엇이 생겼나. 없으면 왜 없는지 |
| 드러난 것을 처분했다 | 아래 「드러난 것의 처분」 |
| 커밋했다 | 청크 하나 = 커밋 하나. 미완이어도 WIP |
| `PLAN`·`PROGRESS` 를 고쳤다 | 분할표의 그 행(결과)과 이력 한 줄(서사). 해시까지 |

## 청크 규칙 — 어떻게 자르나

- 청크 하나 = **번호 절차 3~7단계**, 커밋 1개. 파일 수는 안 센다 — 실측이 3~27개였다(ProjectShop 번들 A, 이곳 `40a`~`42`). diff 는 500줄 안, 마이그레이션·화면 청크는 900. 넘으면 행을 둘로. `PLAN.md`·`PROGRESS.md` 는 안 센다
- 레인은 하나(backend 또는 frontend). API 와 화면은 행을 나눈다(`40a`·`40b`). 마이그레이션은 청크당 하나
- 청크는 서로 독립이어야 한다. 앞 청크가 미완이어도 뒤 청크를 잡을 수 있게 자른다
- 다른 청크를 흡수하면 상한이 그만큼 는다. 어느 청크를 흡수했는지 이력에 밝히고 분할표에서 같이 닫는다
- 중간에 끊기면 `PROGRESS.md` 에 `진행중` 으로 남기고 남은 것을 **파일명 단위**로 적는다
- 커밋 안 된 작업물을 남긴 채 세션을 끝내지 않는다. Stop hook 이 막는다. 버릴 것이면 `git stash`

## 코드를 쓸 때 항상 지키는 것

| 문서 | 무엇을 정하나 |
|---|---|
| `doc/reference/coding-rules.md`(D14) | 계층·예외·트랜잭션·SQL·주석·테스트 |
| `doc/reference/naming-rules.md`(D15) | DB 컬럼·테이블·기본키, Kotlin 식별자 |
| `doc/reference/api-guidelines.md`(D5) | 엔드포인트 — 상태 코드·응답 형식·목록 규약 |
| `doc/reference/concurrency-rules.md`(D4) | 락·격리 수준·멱등키·재시도. **좌석을 건드리면 반드시** |
| `doc/reference/testing-strategy.md`(D8) | 무엇을 어느 층에서 검증하나 |
| `doc/reference/security-baseline.md`(D9) | 인증·입력 검증·비밀 |
| `doc/reference/observability-rules.md`(D10) | 로그 — 개인정보는 식별자만 |
| `doc/reference/frontend-rules.md`(D16) | 화면 코드 — 서버·클라이언트 경계, `api.ts` 입구 |

**전부 읽지 않는다.** 그 청크가 건드리는 것만. 다만 D14·D15 는 코드를 쓰는 모든 청크에 걸린다.
**이식 머리말이 붙은 문서는 원칙만 믿고 예시는 안 믿는다** — 예시가 쇼핑 도메인이다.

**Kotlin 을 쓴다.** Java 식으로 쓰지 않는다 — `data class`·널 안전·확장 함수·`when` 을 쓰고, `!!` 를 안 쓴다 — `checkNotNull(값) { 근거 }`. main 은 detekt 가 세운다(`coding-rules.md`).
ProjectShop 의 Java 를 포팅할 때는 **옮기면서 Kotlin 답게 고치고**, 무엇을 고쳤는지 이력에 한 줄 남긴다.

## 드러난 것의 처분

청크를 하다 계획에 없던 것이 나오면 그 자리에서 셋 중 하나로 처분하고 이력에 밝힌다.

| 처분 | 언제 | 어디에 |
|---|---|---|
| 정보 | 다음에 같은 자리에서 틀릴 것 | `doc/reference/stack.md` |
| 흡수 | 이미 있는 청크가 다룰 것 | 그 청크의 설명을 고친다 |
| 신규 | 어느 청크도 안 다루는 것 | `PLAN.md` 에 청크를 세운다 |

**「나중에 본다」는 처분이 아니다.** 어느 청크가 볼 것인지 적어야 처분이다.

## 마무리·점검

**`/wrapup`** — 이 세션(번들)에서 친 청크 전부를 한꺼번에 보고, 끝 보고를 내고, PR 을 연다. 청크마다 안 한다.
**`/inspection`** — 저장소 전체를 한 축으로 훑는다. 청크와 같은 급이라 분할표에 행이 선다.

## 검증

절차와 명령 표는 `/verify` 스킬에 있다. `bash scripts/verify.sh` 가 건드린 레인(`backend`·`frontend`·`tools`)을 골라 돌리고 도장을 찍는다. 훅 본문은 `scripts/hooks/*.sh` 에 있다 — `settings.json` 은 부르기만 한다. **세 층이다.**

| 층 | 언제 | 무엇 |
|---|---|---|
| 청크 | 커밋 앞 | 빠른 레인. 새 `V*` 가 있으면 `verify.sh` 가 `integrationTest` 를 같이 돈다. Stop hook 과 commit hook(`work/*`)이 이 도장을 본다. 같은 지문을 이미 찍어 뒀으면 그 레인은 안 돈다 |
| 번들 끝 | `/wrapup` | `--full` · 좌석 건드렸으면 `*Concurrency*` · 새 `V*` 있었으면 health 기동 · 화면 있었으면 e2e · 독립 리뷰 · PR. push hook 이 full 도장을 본다 |
| push 뒤 | | CI. 빨가면 다음 청크보다 먼저 친다 |

청크 층을 빼면 번들 끝에서 빨개진 커밋을 이분 탐색하게 된다 — 청크 하나 값이다. 그래서 안 뺀다.

**backend 명령은 `JAVA_HOME="C:/Program Files/Java/jdk-25"` 를 앞에 붙인다.** 이 환경의 기본이 JDK 11 이라 Gradle 이 안 뜬다. 안 붙이면 훅이 막는다.

## 토큰 절약

**낭비와 소모는 다르다.** 같은 파일을 두 번 읽는 건 낭비, 청크를 하나 더 하는 건 소모.

- 이미 읽은 파일 다시 읽지 않는다. 수정 직후 확인용 재읽기도 안 한다
- 큰 파일은 필요한 범위만 읽는다
- 「어디에 정의됐나」류 탐색은 서브에이전트에 위임한다
- 검색 시 `node_modules/`·`build/`·`.next/` 제외
- 빌드·테스트 출력은 실패한 부분만 인용한다

## 글 작성 규칙 (문서·주석·커밋)

화면 문구는 예외다 — 존댓말이고 `screen-rules.md` 가 정한다. 아래는 개발자가 읽는 글에만 걸린다.

1. **중복 금지** — 같은 내용을 두 번 쓰지 않는다
2. **사견 배제** — 사용자가 실제로 한 말과 결정만 적는다
3. **늘어지지 않게** — 필요한 만큼만
4. **존댓말 금지** — `~습니다` `~합니다` `~하세요` `~입니다` 금지. 평서형. `doc-lint.sh` 가 잡는다
5. **설명은 6하원칙** — 첫 문장에 `무엇을` `무엇으로` `어떻게`
6. **굵게는 결정에만** — 사실 나열에 안 쓴다

## 이 저장소 밖의 연결점

`C:\workspace\ProjectShop` — 이식 원본. 무엇을 가져오고 무엇을 안 가져왔나는 ADR 0002.
