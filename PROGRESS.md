# 진행 로그

## 현재 상태

**대상**: 공연 예매 시스템 (Kotlin + Spring Boot 4 + Next.js, 로컬 전용). **가지**: `work/2026-09-16-a`

| 무엇 | 상태 | 다음 손 |
|---|---|---|
| 진행중 청크 | 없음 | |
| 다음 첫 손 | **Opus**: `26` 알림(소비자 1) → `27` 정산(소비자 2). **Fable**: `27` 뒤 ADR 0007 | `PLAN.md` 의 `26` 행 |
| PR | `#11`~`#14` 머지됨. 이 가지는 `13a` | 마무리 |
| 의존성 | Kotlin 플러그인은 Boot BOM 에 묶여 dependabot `ignore` — 이름은 `jvm`·`plugin.spring`(축약 표기 때문). Boot 가 BOM 을 올리면 지운다 | `.github/dependabot.yml` |
| 확정된 수치 | ADR 0003(도메인 수치 + **정산·환불 시작값**), ADR 0004(세션·`paying`·관문·회차 종료) | 그 문서들 |
| GitHub 저장소 | `https://github.com/ejg93/ProjectTicket`. `main` 가지 보호 걸림(네 잡 필수). 그 뒤로는 `work/<날짜>` + PR | |
| 로컬 포트 | ProjectShop 컨테이너가 5432·6379 를 쥐고 있어 `.env` 는 5433·6380 | `README.md` |
| 기준 문서 | 20개 중 **18 완료**. 남은 셋 — D13(31 뒤), D16·D17(화면, 41 뒤), D18(품질 게이트, 46·47 뒤). 전부 Fable | `P9`·`P10`·`P11` |
| 이식 원본 | `C:\workspace\ProjectShop` 읽기 전용. 가져온 것·안 가져온 것은 ADR 0002 | |

## 이력

| 날짜 | 청크 | 결과 | 커밋 |
|---|---|---|---|
| 2026-09-14 | 0. 저장소 뼈대 | 완료 — `CLAUDE.md`·`PLAN.md`·`PROGRESS.md`·`README.md`·`docker-compose.yml`·`.claude/settings.json`·스킬 4·`scripts/` 3·`.github/` 신설. ADR 0001(스택)·0002(이식 범위). `doc/reference/` 15개를 머리말 달아 원문 이식, `domain-model.md`·`glossary.md` 신설. **왜**: 사용자가 Kotlin+Spring 을 골랐고(국내 공고 60~70% Java+Spring, 주요 IT 가 Kotlin), Kafka 는 소비자 둘째에서, k8s 는 로컬 kind, AWS 는 안 한다. ProjectShop 은 docs 313 대 feat 142 라 문서 비중을 낮추기로 했다(대전제 5). **검증**: `bash scripts/doc-lint.sh` 통과 | cb20edf |
| 2026-09-14 | 1. backend 골격 | 완료 — `backend/build.gradle.kts`·`settings.gradle.kts`·Gradle 래퍼(ProjectShop 것), `TicketApplication.kt`·`health/HealthController.kt`, `application.yml`·`application-local.yml`, `V1__baseline.sql`, `PostgresTestBase.kt`·`HealthControllerTest.kt`, `backend/README.md`·`backend/CLAUDE.md` 신설. **왜**: 축은 관례 — Kotlin 버전은 Boot 4.1.1 BOM 이 관리하는 2.3.21 에 맞췄고(플러그인은 BOM 밖이라 직접 적는다), `-Xjsr305=strict` 로 Spring 의 널 어노테이션을 Kotlin 타입에 반영한다. 테스트 레인은 ProjectShop 과 같이 태그로 가른다 — 컨테이너 유무가 `PostgresTestBase` 상속에 따라오므로 빠뜨릴 자리가 없다. ProjectShop 의 fork 별 DB 분리·Redis 컨테이너·bcrypt 비용 조정은 안 가져왔다 — 쓰는 청크(13·21·3)에서 들인다. **달랐던 것**: `@ServiceConnection` 을 썼다 — ProjectShop 은 fork 별 DB 때문에 못 썼지만 여기는 아직 fork 가 하나다. 로컬 5432·6379 를 ProjectShop 컨테이너가 쥐고 있어 `.env` 를 5433·6380 으로(정보 → `README.md`). **검증**: `JAVA_HOME=jdk-25 ./gradlew build` `BUILD SUCCESSFUL` — `test` 0개(빠른 레인 비어 있음), `integrationTest` 1개 통과. `bootRun` 뒤 `curl /api/health` → `{"app":"ticket-backend","database":"ticket","applied_migrations":1,…}`. `verify.sh` 는 `origin/main` 이 없어 못 돌렸다 | 1f8bcb6 |
| 2026-09-14 | 2. CI 첫 초록 | 완료 — `backend/gradlew` 모드 100755, `main` 가지 보호(API). **왜**: 축은 관례(GitHub Actions). 첫 push 의 CI 가 둘 빨갰다 — backend 는 `./gradlew: Permission denied`(126, Windows 엔 실행 비트가 없어 로컬에선 안 난다), secrets 는 gitleaks 가 루트 커밋의 부모(`cb20edf^`)를 찾다 죽은 것으로 **첫 push 에만 나는 것이라 안 고쳤다** — 다음 push 에서 저절로 초록이 됐다. 가지 보호는 관리자를 안 막는다 — CI 설정 자체가 깨졌을 때 잠기지 않게. **달랐던 것**: push 훅이 명령 문자열을 실행 전에 검사해서 `git checkout -b … && git push` 를 한 명령에 넣으면 아직 `main` 이라고 막는다 — 가지 따기와 push 를 나눠 부른다(정보). **검증**: run `34819188373` 네 잡 초록. `verify.sh` 는 지문(코드 블롭)이 `origin/main` 과 같아 돌릴 것이 없다 | 27a4d2c |
| 2026-09-14 | 3. 계정·인증 포팅 | 완료 — `V2__account.sql`, `error/` 5개, `auth/` 10개, `account/MeController.kt`, `ArchitectureTest.kt`·`LoginFlowTest.kt`·`ProblemDetailTest.kt` 신설. `PostgresTestBase` 에 컨텍스트 소거·bcrypt 4, `application.yml` 에 세션 쿠키, `build.gradle.kts` 에 security·archunit. **왜**: 축은 표준(RFC 9457·RFC 9110 §15.5.2·RFC 5321·NIST 800-63B) + `D9`. **권한 다섯 테이블 대신 `account.role` 컬럼 하나** — 역할이 셋이고 행 단위 스코프가 없어서(ADR 0002) 컬럼 check 가 가장 낮은 강제 지점이다. 기획사 소속은 `organizer_member`(청크 7)가 든다. Kotlin 으로 옮기며 고친 것: enum `of` 를 `entries.firstOrNull`, 필터의 `principal` 을 `is` 스마트캐스트, `TicketUser` 는 `data class` 가 아니다(해시를 지워야 해서). 로그인 실패 카운터는 Redis 가 필요해 `3a` 로 세우고 `21` 뒤에 뒀다. 동의·감사는 `4` 가, `trace_id` 는 `5` 가 이 자리에 붙인다(흡수). **달랐던 것**: Kotlin MockMvc DSL 에서 지역 변수 `session` 이 DSL 속성을 가려 `val cannot be reassigned` — 이름을 바꿨다(정보). 401 본문의 `Content-Type` 에 `charset` 이 붙어 `contentTypeCompatibleWith` 로 봤다. **검증**: `gradlew build` 초록 — 빠른 레인 3(ArchitectureTest), 느린 레인 11. `verify.sh --full` 도장 | 502ca28 |
| 2026-09-14 | 마무리 2차 | 완료 — 대상 청크 3. `doc-lint.sh` 통과, 대조 다섯에서 둘: `auth/` 파일 수 오기(11→10, 분할표·이력 고침), `ProblemDetailTest` 의 테스트 이름이 단언(401)과 달라 이름·주석을 고침. **독립 리뷰: 지적 18, 처분 — 지금 고침 16 · 새 청크 1(`3b` 블록리스트) · 오탐 1**. 고친 것: 401 셋(로그인 실패 핸들러·생존 필터·동시 세션 필터)에 `WWW-Authenticate` 와 problem 본문이 없었다 → 핸들러는 `ResponseEntity` 로, 필터 둘은 `ProblemEntryPoint.commence` 를 부른다. 403 에 `accessDeniedHandler` 가 없어 CSRF 실패가 기본 본문으로 나갔다 → `ProblemAccessDeniedHandler` 신설(`FORBIDDEN` 이 살아났다). 정지 계정을 `isEnabled=false` 로 걸면 bcrypt 전에 거절돼 **응답 시간으로 존재가 샌다** → `isEnabled` 는 항상 true, 정지 판정은 인증 뒤 컨트롤러에서 같은 문구로. `changeSessionId()` 가 생성 시각을 안 바꿔 절대 만료 12h 가 익명 세션 시각부터 셌다 → 로그인 앞에서 세션을 버리고 새로 만든다. 세션 고정 설정이 두 곳(하나는 죽은 설정) → 전략 빈 하나로. 기본 `LogoutFilter` 가 살아 `POST /logout` 이 302 로 답하던 둘째 경로 → 끔, 우리 로그아웃이 `CsrfLogoutHandler` 로 쿠키까지 지움, 로그인 뒤 CSRF 토큰 회전(`CsrfAuthenticationStrategy`). 합성 제약의 `message` 가 `@ReportAsSingleViolation` 없이 죽은 글 → 붙임. 이메일 254 를 `length`(글자)로 재던 것 → `octet_length`(RFC 5321 단위). `data class` 요청의 `toString` 이 평문 비밀번호를 찍음 → 재정의. 생존 필터 SQL 의 `'active'` 리터럴 → `AccountStatus` 로. 프레임워크 오류(406·413·503)의 본문 `status` 가 HTTP 상태와 달랐다(RFC 9457 §3.1) → 실제 상태로. `URI.create(requestURI)` 가 던지면 마지막 그물이 무너짐 → `runCatching`. 메서드 보안의 `AccessDeniedException` 을 500 으로 삼키던 것 → 핸들러 추가. ArchUnit 주석이 규칙보다 넓게 말함 → 주석 좁히고 `HttpStatus` 규칙(error·Controller 밖 금지) 추가. CSRF 진짜 경로가 테스트에 없음 → `CsrfCookieTest` 2개. 레지스트리 만료를 부르는 곳이 없는데 주석·테스트가 있는 것처럼 말함 → 「자리만 있다」로 고침. **오탐**: NIST 15자 SHALL 인용은 맞다(리뷰어가 「모름」). **검증**: `gradlew build` 초록 — 빠른 4, 느린 14. `verify.sh --full` 도장 | 91a6beb |
| 2026-09-14 | 5. 관측 포팅 | 완료 — `observability/RequestLogFilter.kt`·`logback-spring.xml`·`RequestTraceTest.kt` 신설, `ProblemFactory`·`application.yml`·`build.gradle.kts`·`stack.md` 수정. **왜**: 축은 `D10` + 표준(W3C Trace Context). **`4` 보다 먼저 잡았다** — 감사 행이 추적 ID 를 남기는데 그 값이 여기서 생겨서, 순서를 뒤집으면 `4` 를 두 번 건드린다. Brave 기본 전파가 B3 라 `w3c` 로 바꿨다 — 자체 헤더를 쓰면 관측 도구를 붙이는 청크 30 에서 갈아엎는다. 로그에 `traceId` **뒤** 6자리를 찍는다: Brave 의 앞 8자리가 생성 시각이라 앞자리를 찍으면 같은 초의 요청이 안 갈린다. 필터는 보안 필터 **바깥**에 둔다(`HIGHEST_PRECEDENCE + 10`) — 안쪽이면 401·403 이 한 줄도 안 남고, 정작 되짚어 볼 것이 거부와 실패다. **달랐던 것**: `trace_id` 가 요청마다 달라져 `LoginFlowTest` 의 **본문 통째 비교**가 깨졌다 — 「두 실패가 같은 것을 말하나」는 `type`·`detail` 이 답하므로 그 둘만 비교한다. `CsrfCookieTest` 가 `build` 한 번에서 「쿠키가 없다」로 깨졌고 **일곱 번을 더 돌려도 재현이 안 됐다** — 원인을 못 짚어 안 고치고, 그 테스트가 상태 코드를 먼저 보게 해서 다음에 나오면 무엇이 틀렸는지 말하게 했다(정보 → `stack.md`). **검증**: `gradlew build` 초록 — 빠른 4, 느린 20. 손 줄(`/verify` 「로그·추적」): 기동 후 401 본문의 `trace_id` 가 `6aa7…7a32a0` 이고 그 요청의 로그 줄이 `[7a32a0,5c29]` 로 뒤 6자리가 같다. 같은 초의 세 요청이 `831164`·`7def60`·`447b74` 로 갈리고 `/actuator/health` 는 한 줄도 안 남았다. `verify.sh --full` 도장 | 9828e19 |
| 2026-09-14 | 결정 묶음 | 완료 — `doc/adr/0003-domain-decisions.md` 신설. **왜**: 청크를 연속으로 치려면 중간에 멈추는 자리를 미리 없애야 해서, 「갈리면 묻는다」에 걸릴 것을 **치기 전에** 한꺼번에 물었다. 사용자가 고른 것: 취소 수수료 국내 통상 5구간, 부분 취소 불가, 1인 4매, 대기열 **항상 켬**(경로가 둘로 갈리면 꺼진 쪽이 덜 돌아간다), 5초/100명·TTL 10분, 순번은 폴링 2초(SSE 는 인스턴스에 묶인다), 좌석도 SVG(Canvas 는 `axe` 가 읽을 DOM 이 없어 닫힘 조건을 못 닫는다), 등급은 구역 단위, 모의 결제는 입력으로 유도, 마무리는 묶음 끝에 한 번, 나머지 기본값 18개는 추천대로. **검증**: `doc-lint.sh` 통과 | ca03a13 |
| 2026-09-14 | 4. 감사·동의 포팅 | 완료 — `V3__audit_consent.sql`, `audit/AuditLog.kt`·`AuditLogWriter.kt`, `consent/ConsentService.kt`·`ConsentController.kt`, `AuditImmutabilityTest`·`LoginAuditTest`·`ConsentSignupTest` 신설. `SignupService`·`AuthController`·`SecurityConfig`·`ErrorCode`·`PostgresTestBase` 수정. **왜**: 축은 `D9` + 개인정보 최소수집. **감사 행의 `delete` 를 보존 3년으로 가른다** — 전부 막으면 파기 배치가 못 돌고 전부 열면 은폐가 된다. `ATTEMPT`/`OUTCOME` 을 가른 것은 **실패한 로그인이 롤백에 쓸리면 안 되기 때문**이고, 그래서 전파를 바꿀 자리가 따로 필요해 `AuditLogWriter` 를 뗐다(자기 호출은 프록시를 안 지나 `REQUIRES_NEW` 가 무시된다). 동의는 **담긴 것만** 적어 거부와 무응답을 가른다. 실패 감사에 **이메일을 안 담는다** — 파기 예외로 3년 남는 자리에 가입도 안 한 사람의 주소가 남을 이유가 없다. 동의 항목은 셋이다(ADR 0003) — 야간 광고 동의를 안 만든 이유는 물어볼 채널이 없어서다. **달랐던 것**: 보존 가드가 **테스트 정리를 막아서 22개가 한꺼번에 빨개졌다** — 가드가 제 역할을 한 것이라 가드를 안 고치고, 정리 쪽이 `set local session_replication_role` 로 그 트랜잭션에서만 트리거를 끈다. 가드 자체는 `AuditImmutabilityTest` 가 따로 재서 검증이 비는 자리가 없다. `JdbcClient.list()` 의 원소가 플랫폼 타입이라 Kotlin 이 `List<T?>` 로 본다(정보). **검증**: `gradlew build` 초록 — 빠른 4, 느린 30 | 652ac66 |
| 2026-09-14 | 6. 이식 `P1`·`P2` | 완료 — `coding-rules.md`·`naming-rules.md` 재작성, 이식 머리말 삭제. **왜**: 축은 규약 — 바깥 표준을 옮겨 적는 것이 아니라 **코드에 이미 성립한 관례**를 뽑는 자리다. 갈림길에서 사용자가 **「실물 기준으로 새로 쓴다」**를 골랐다: 원문 864줄에 쇼핑 도메인 예시 45곳·ProjectShop 문서번호 23곳이 있었고, 어긋난 자리만 고치면 **여기 없는 기능(정산·반품·다중 셀러)을 규율하는 절이 그대로 남아** 읽는 사람이 어디까지 진짜인지 모른다. 49KB+11KB 를 17KB+4KB 로 줄이면서 뼈대는 살렸다 — 특히 **규칙의 우선순위(축 1 출처, 축 2 강제 지점)** 와 「플랫폼 사실은 순위 다툼에 안 들어온다」는 이 저장소의 판단 기준이라 그대로 가져왔다. Kotlin 절을 새로 썼다: `when` 에 `else` 를 안 두는 이유, 플랫폼 타입을 `!!` 로 안 덮는 이유, 자기 호출로 전파가 안 걸리는 것. **검증**: `doc-lint.sh` 통과. 코드 지문이 안 바뀌어 `verify.sh` 는 돌릴 것이 없다 | afb1183 |
| 2026-09-14 | 7. 기획사·공연장 스키마 | 완료 — `V4__venue.sql`·`venue/SeatUniquenessTest.kt` 신설, `HealthControllerTest` 수 올림, `CsrfCookieTest`·`stack.md` 수정. **왜**: 축은 `D2`·`D15` + 강제 지점 2위. 소속에 **트리거를 양쪽에 건다** — 넣을 때만 보면 역할을 뒤늦게 내려서 같은 구멍이 열린다. `seat` 를 수정·삭제하지 않는 이유는 회차가 그 행을 복제하기 때문이고(청크 9), 배치가 바뀌면 홀을 새로 만든다 — 지난 회차는 지난 홀을 그대로 가리키는 것이 사실이다. 열은 글자다(`A`·`B`) — 숫자로 굳히면 그렇게 매기는 공연장을 못 담는다. **달랐던 것 둘**. 하나, **한 테스트에 제약 위반을 둘 넣으면 둘째가 못 잰다** — 첫 위반이 트랜잭션을 어보트시켜 `25P02` 가 나온다. 위반 하나당 테스트 하나로 갈랐다(정보). 둘, **`CsrfCookieTest` 의 간헐을 잡았다** — `csrf()` 후처리기가 공유 `CsrfFilter` 의 저장소를 테스트용으로 바꿔 끼우고 **그것이 컨텍스트에 남아서**, 그 테스트가 먼저 돈 순서에서만 우리 쿠키 저장소가 안 돌았다. 실패 문구에 저장소 이름을 실어 잡았고, 재는 자리가 스스로를 지키게 `@BeforeEach` 에서 되돌린다(`stack.md`). 헤더와 쿠키가 어긋나면 403 인 것도 테스트로 더했다. **검증**: `gradlew build --rerun-tasks` **3회 연속** 전부 초록 — 빠른 4, 느린 41 | ef8d886 |

| 2026-09-14 | 8. 공연·회차·등급 | 완료 — `V5__event.sql`·`event/EventFixture.kt`·`PerformanceStateTest.kt` 신설. **왜**: 축은 `D3`(회차 상태) + 강제 지점 2위. **등급은 공연 단위, 매핑은 구역 단위**(ADR 0003) — 가격은 공연이 정하는 것이고 같은 공연의 회차끼리 갈릴 이유가 없다. 가격을 회차 오픈 때 박제하는 이유는 **산 사람과 살 사람의 가격이 갈리면 안 되기 때문**이다. 상태 전이를 트리거로 묶었다 — 코드에만 두면 `psql`·배치로 들어오는 변경을 못 막고, 되돌리기가 특히 위험하다. 등급이 그 공연의 것인지는 **복합 외래키로 못 걸어서**(`seat_grade` 기본키가 단일 열) 트리거가 본다. **달랐던 것**: `unknown_status_is_rejected` 를 `update` 로 재려 했더니 **전이 트리거가 check 보다 먼저 걸려서** 값 목록이 아니라 전이표를 재고 있었다 — 삽입으로 바꿨다(정보). **검증**: `gradlew build` 초록 — 빠른 4, 느린 49 | 7cccb60 |
| 2026-09-14 | 9. 회차 오픈 = 좌석 복제 | 완료 — `V6__performance_seat.sql`·`event/PerformanceOpenService.kt`·`PerformanceOpenTest.kt` 신설, `ErrorCode` 둘 추가. **왜**: 축은 `D2` + 강제 지점 2위. 복제와 상태 변경이 **한 트랜잭션**이다 — 갈리면 아무도 못 사는 좌석 수천 행이 남거나 **좌석이 하나도 없는 열린 회차**가 된다. 복제는 한 문장이다(행마다 왕복하면 2천 석에 2천 번이고 그동안 트랜잭션이 열려 있다). **상태와 부속 값이 어긋나는 것을 check 로 묶었다** — `held` 인데 만료 시각이 없으면 스윕이 그 좌석을 영영 못 되돌리고, `available` 인데 예매를 가리키면 두 번 팔린 것처럼 보인다. **등급이 안 붙은 구역은 스키마로 못 본다** — 매핑은 공연 단위고 홀은 회차가 정하므로 둘이 만나는 순간에야 알 수 있어서 서비스가 막는다. **달랐던 것**: `jsonb` 를 텍스트로 읽으면 `{"seat_count": 2}` 처럼 콜론 뒤에 공백이 붙는다(정보). **검증**: `gradlew build` 초록 — 빠른 4, 느린 55 | 72cc056 |

| 2026-09-14 | 마무리 3차 | 완료 — 대상 청크 5·4·6·7·8·9. `doc-lint.sh` 통과, 문서가 부르는 `V*`·테스트 이름을 실물과 대조(어긋남 없음. 없는 넷은 미착수 청크의 예보다). **독립 리뷰: 지적 20, 처분 — 지금 고침 14 · 새 청크 1(`4a`) · 흡수 3(청크 13 행) · 오탐 2**. 고친 것 가운데 큰 것: **소속 가드 둘이 TOCTOU 였다** — 서로의 미커밋을 못 봐서 「역할은 관객인데 소속된 계정」이 READ COMMITTED 에서 만들어졌고, 삽입 트리거가 `for no key update` 로 계정 행을 잠근다. 같은 트리거가 **없는 계정에 무동작**이었다(`null not in (…)` 이 null). **회차를 `insert … status='open'` 으로 만들 수 있었다** — 전이 트리거가 `update` 만 봐서 오픈 절차를 통째로 지나칠 길이 있었고, 삽입 가드를 세웠다. **오픈이 경합에서 500 을 냈다** — 잠그지 않고 상태를 읽어 진 쪽이 유일 제약 위반으로 죽었고, `for update` 로 바꿔 422 가 나간다. **오픈 검사와 복제 사이가 벌어져 있었다** — 매핑이 지워지면 내부 조인이 그 구역을 조용히 빠뜨려서, 복제 수를 홀의 좌석 수와 맞춰 본다. **`seat` 와 `seat_grade.event_id` 가 「고치지 않는다」면서 아무것도 안 막았다** — 트리거를 세웠다. **`account_consent` 가 append-only 라면서 `audit_log` 와 달리 무방비였다** — `update` 트리거를 세웠다. `truncate` 는 행 트리거에 안 걸려서 문 트리거를 더했다. 동의 값이 `null` 이면 not null 컬럼에 부딪쳐 **500** 이던 것을 422 로. 정지 계정 로그인 실패가 계정 번호 없이 남던 것을 채웠다(그 자리는 계정이 밝혀진 자리다). 요청 로그가 예외로 빠져나갈 때 **상태를 200 으로 찍던** 것을 예외 이름으로. **오탐 둘**: `seat_grade_map` 의 없는 구역 오타는 무해하다(내부 조인이 아무것도 안 만들고 커버리지 검사는 홀 기준이다), `reservation_id` 외래키 부재는 아직 쓰는 코드가 없어 청크 13 이 걸면 된다. **감사 트리거가 테이블 주인에게는 무력하다는 것을 주석에 밝히고** 권한 분리를 청크 `4a` 로 세웠다 — 막을 수 있다고 적어 두는 것이 안 막는 것보다 나쁘다. **달랐던 것**: 마이그레이션 셋이 아직 안 풀린 가지라 제자리에서 고치고 재사용 컨테이너를 지웠다 — 적용된 파일을 고치면 체크섬이 안 맞는다(`D14`). 삽입 가드가 `performance_status_check` 를 가려서 그 check 는 트리거가 꺼진 자리에서만 보이는 아래 그물이 됐다. **검증**: `gradlew build` 초록 — 빠른 4, 느린 61 | 49b5ee0 |
| 2026-09-14 | 세션 마무리 | 완료 — `.github/dependabot.yml` 에 Kotlin 플러그인 `ignore`, 「현재 상태」를 머지 뒤로 고침. **왜**: dependabot 이 Kotlin 플러그인 2.3.21 → 2.4.20 을 열었는데, **Boot 4.1.1 BOM 이 stdlib 을 2.3.21 로 고정**해서 플러그인만 올리면 컴파일러와 stdlib 이 다른 판이 된다 — `build.gradle.kts` 주석이 이미 경고하던 자리다. 보안 이유가 없어 **사용자가 「BOM 값에 맞춘다」를 골랐고** PR 을 닫았다. **달랐던 것**: 그 PR 의 backend 가 빨간 이유는 Kotlin 이 아니라 **옛 `gradlew` 실행 비트**였다(청크 2 의 수정 전 `main` 에서 갈라졌다) — 빨간 검사를 보고 호환성 문제로 단정할 뻔했다. 「현재 상태」가 머지 전 내용으로 남아 있어 다음 세션이 이미 머지된 PR 을 열려 했을 자리도 고쳤다. **검증**: `doc-lint.sh` 통과 | 1220069 |

| 2026-09-15 | 설계 점검 | 완료 — `doc/adr/0004-design-review.md` 신설, `PLAN.md` 재편(새 행 13: 12a~12f·16a·17a·20a·5a·5b·35a·D20, 고친 행 13·16·17·19·20·25·28·33·34). **왜**: 청크 9 뒤에 설계 전체를 다시 봤더니 **13 이후를 지탱할 결정이 문서에 없었다** — 세션이 JVM 안에 있어 인스턴스 3대를 못 넘고, 예매 상태기계가 없어 스윕과 승인이 경합하며, 동시성 규약(D4)이 13 뒤(P5)에 있고, 예매 생성에 멱등키가 없고, 대기열 관문 범위·정원 등식·반납이 미정이고, 좌석 조회가 초당 5천 폴링을 DB 직결로 받으며, 동시성 테스트가 `@Transactional` 바탕에서 못 돌고, 회차의 끝(마감·자동 종료·취소)이 없고, 탈퇴·파기·관리자가 없다. **사용자가 넷을 골랐다**: 세션은 Spring Session Redis(JWT 는 즉시 취소 불가, sticky 는 문제를 숨긴다), 결제 중은 `paying` 상태 + 3분 타임아웃(선점 연장은 「결제 중인가」를 묻는 자리가 없다), 관문은 선점만(TTL 이 결제 중에 죽어도 된다), 회차는 `sales_close_at` + 자동 종료 + 회차 취소 청크. **설계 청크 다섯을 코드보다 앞에 세웠고 넷을 이 세션에서 썼다.** ADR 번호를 재배정했다(19→0006, 28→0007, 34→0008). **검증**: `doc-lint.sh` 통과 | 115cf43 |
| 2026-09-15 | 12a. `D3` 예매 상태기계 | 완료 — `state-machines.md` 재작성(P6 흡수, 머리말 삭제). **왜**: 축은 `D2` + 표준(경합을 상태로 푼다). `held→paying→reserved→cancelled`, `held/paying→expired`. **`paying→held` 는 되돌리기가 아니라 재시도 허용**이다 — 카드가 거절돼도 선점 시간이 남았으면 다른 카드로 다시 시도하고 `held_until` 은 안 늘린다. 좌석 상태는 예매의 투영이고 대응표를 뒀다. 회차에 `cancelled` 를 더했다(17a 가 check·트리거에 넣는다). 상태 이력 표를 안 두고 열 셋(`reserved_at`·`cancelled_at`·`expired_at`)과 감사로 대신한다 — 분쟁이 묻는 것은 그 둘뿐이다. 관리자 강제 전이를 안 만든다. **검증**: `doc-lint.sh` 통과 | fc1d837 |
| 2026-09-15 | 12b. `D4` 동시성 규약 | 완료 — `concurrency-rules.md` 재작성(P5 흡수, 머리말 삭제). **왜**: 축은 표준(Postgres §13.2.1 원문 — 잠금 대기 뒤 `WHERE` 재평가). 그것이 Read Committed 에서 조건부 UPDATE 가 「승자 하나」를 주는 근거고, 0행은 재시도가 아니라 확정 답이다. **한 문장 `UPDATE … ANY(ids)` 는 잠금 순서를 보장하지 않아** 겹치는 요청이 교착(40P01)한다 — id 오름차순 `select … for update` 를 먼저 건다. 문장 순서는 `V6` 의 check 가 정했다(예매 행 → 잠금 → UPDATE → 행 수 → `reservation_seat`). `reservation_seat` 은 기록, `reservation_id` 는 포인터 — 같은 것 두 벌이 아니라 과거와 현재다. 합계는 **지연 제약 트리거**(예매 행이 좌석보다 먼저 들어와서 즉시 트리거는 0 과 대조한다). 멱등키는 선점·결제에만 필수, 취소는 조건부 UPDATE 가 대신한다. 결제는 트랜잭션 셋(PG 호출은 밖). 읽기 모델 갱신은 커밋 뒤. **검증**: `doc-lint.sh` 통과 | a177390 |
| 2026-09-15 | 12c. `D12` 대기열 설계 | 완료 — `queue-design.md` 신설. **왜**: 축은 표준(OWASP 토큰) + 측정 등식. **대기열이 막는 것은 처리량이 아니라 순간 폭발**이다 — R=20/s 는 DB 상한(풀 10 / 20ms = 500/s) 의 4% 고, 줄이 없으면 오픈 초에 Tomcat 스레드가 차고 9,800 명이 커넥션 대기에서 죽는다. ZSET member 가 계정이라 `ZADD NX` 한 글자가 「새로고침해도 순번 유지」와 「봇 중복 진입 차단」을 같이 준다. 입장은 Lua 원자라 인스턴스 셋이 돌아도 한 명이 두 번 안 들어간다. **토큰은 무작위 32바이트지 서명 토큰이 아니다** — 반납할 수 있어야 해서다. 관문은 선점만(ADR 0004), 선점 성공 시 즉시 반납, 실패 시 유지. Redis 죽으면 관문이 **닫힌다**(503) — 열면 대기열이 지키려던 폭발이 DB 로 간다. 정원 C=2,000 은 「입장했는데 안 사는 사람」이 쌓이는 것만 막는다. **검증**: `doc-lint.sh` 통과 | 650f672 |
| 2026-09-15 | 12d. `D20` 좌석 읽기 모델 | 완료 — `seat-read-model.md`·`doc/adr/0005-seat-read-model.md` 신설, `domain-model.md` 수정. **왜**: 축은 `D5` + 표준(RFC 9110 조건부 요청). 저울질 넷 — DB 직결+ETag(304 를 주려고 200 만큼 읽는다), 회차 행 버전 컬럼(선점마다 같은 행을 갱신하는 **뜨거운 행**), SSE(인스턴스에 묶인다), **Redis 버전+스냅샷+델타**(골랐다). 버전은 커밋 **뒤** `INCR` — 앞에 올리면 롤백된 선점이 화면에 보인다. 스냅샷은 DB 에서 만들어 Redis 가 죽어도 **느려질 뿐 틀리지 않는다**. 상태는 한 글자·키는 짧게 — 2천 석 × 폴링이라 필드 이름이 대역폭이다. `since` 가 로그 밖이면 410 이고 전체 재로드. **검증**: `doc-lint.sh` 통과 | 62d85c1 |

| 2026-09-15 | 의존성 ignore 수정 | 완료 — `.github/dependabot.yml` 이름 수정, `#6` 을 `@dependabot ignore this dependency` 로 닫음. **왜**: `#5` 가 `org.jetbrains.kotlin*` 을 넣은 **4분 뒤에 `#6` 이 열렸다** — Dependabot 은 `kotlin("jvm")` 축약 안의 문자열(`jvm`·`plugin.spring`)을 이름으로 본다. 패턴을 그 이름으로 맞췄고, 서버 쪽 ignore 명령도 같이 걸어 yml 이 또 어긋나도 이 갱신은 안 열린다. 축약을 전체 id 로 푸는 길은 빌드 파일을 건드려서 안 골랐다. **검증**: `doc-lint.sh` 통과. 다음 월요일 실행에서 안 열리는 것으로 최종 확인 | 0fb8144 |

| 2026-09-15 | P4. `D8` 테스트 전략 | 완료 — `testing-strategy.md` 재작성(머리말 삭제). **왜**: `12f` 가 바로 다음인데 그 바탕의 모양이 문서에 없었다. 레인을 셋으로 — **측정 레인(`measure`)을 `build` 밖에** 뒀다: 결과가 초록·빨강이 아니라 숫자고 CI 러너의 숫자는 뜻이 없다. `ConcurrencyTestBase` 는 트랜잭션만 없는 통합 층이다 — 서블릿이 아니라 **커밋**이 필요한 것이라 층을 안 옮긴다. 「승자 하나」를 응답 둘·DB 둘로 잰다 — 응답만 보면 「둘 다 201 인데 DB 는 하나」를 놓친다. **정렬을 지워도 통과할 수 있어서** 잠금 순서를 손으로 엇갈리게 만든 대조 테스트를 같이 둔다. 만료 테스트는 시계가 DB 라 `Clock` 고정이 안 되고 **지난 시각의 행을 직접 넣는다**. 이 저장소가 밟은 것 셋(제약 위반은 하나씩, 트리거가 check 보다 먼저, `csrf()` 저장소 바꿔치기)을 규칙으로 올렸다. **검증**: `doc-lint.sh` 통과 | 54f84b8 |
| 2026-09-15 | P3. `D5` API 규약 | 완료 — `api-guidelines.md` 재작성(머리말 삭제). **왜**: 10·13·16·17·23 이 각자 슬러그를 지어내기 전에 **오류 `type` 목록을 계약으로** 굳혔다 — 기존 12 + 새 17(`seat-taken`·`over-limit`·`admission-required`·`invalid-transition` 등), 추가 필드까지. **갈림길 하나를 그 자리에서 정했다**: 열거값 표기. 원문(Zalando)은 대문자 스네이크인데 DB 가 소문자라 로그·쿼리·응답을 한 단어로 검색하려면 JSON 도 소문자여야 하고, 그것이 snake_case 를 고른 이유와 같다 — **소문자로 통일하고 D3·D4·ADR 0004·PLAN 의 `held` 를 `held` 로 맞췄다.** 선점 경로를 `/api/performances/{id}/reservations` 로 — 관문 필터가 회차 id 를 본문이 아니라 경로에서 읽어야 스트림을 두 번 안 읽는다. 409 와 422 의 경계를 「다른 시점이면 통과했나」로. 예매·티켓은 남의 것이면 404(판매량 지도가 새면 안 된다). **검증**: `doc-lint.sh` 통과 | fb25569 |
| 2026-09-15 | P7. `D7` 시각 규약 | 완료 — `time-rules.md` 재작성(머리말 삭제). **왜**: 14 스윕의 기준 시각과 17 의 구간 경계가 이 문서 몫인데 17 뒤에 있었다. **시계는 DB 다** — 인스턴스 셋의 JVM 시계가 어긋나면 한 대가 넣은 `held_until` 을 다른 대가 「아직」이라 본다. 만료·마감·타임아웃은 전부 SQL `now()`, 앱 `Clock` 은 순수 계산기에만. 「관람일 N일 전」= KST 달력일 차 — 시각(24h)으로 세면 저녁·낮 공연의 경계가 갈린다. **`at time zone` 을 빼면 한국 0~9시의 취소가 전날로 잡힌다** — 이 문서를 쓴 이유다. 박제 다섯(선점·결제·마감·수수료율·가격). 영업일·공휴일은 정산 규약이 필요로 할 때. 청약철회 예외 조문은 미확인이라 `D6` 로 넘겼다. **검증**: `doc-lint.sh` 통과 | 4624ce4 |

| 2026-09-15 | D21·D6 정산·환불 규약 | 완료 — `settlement-rules.md`·`refund-policy.md` 신설, ADR 0003 에 정산·환불 절 추가, 27·17 행 수정. **왜**: 청크 27 이 「집계」라고만 적었고 **수수료율이 프로젝트 어디에도 없었다.** 사용자가 「선택에 따라 코드가 많이 바뀌면 고르기 어렵다」고 해서 **구조를 먼저 골랐다** — 값은 `settlement_policy` 표에, 정산서는 `settlement_line` 의 합. 그러면 요율·귀속·D+N 은 결정이 아니라 시작값이고 행 하나로 바뀐다. 진짜 결정은 **회차 단위 D+7** 하나다 — 당일 취소가 안 되므로 회차 종료 뒤엔 취소가 없고, D+7 정산서가 보는 상태가 최종이라 「정산 뒤 환불 차감」이 구조에서 사라진다. 회차 취소는 0원 정산서를 만든다(회차당 한 장 규칙). 환불은 예매 단위에 한 번 반올림 — 좌석마다 하면 합이 안 맞는다. 청약철회 제한 근거(전자상거래법 제17조제2항, 사전 고지 요건)를 확인해 고지 자리 둘을 적었고, 소비자분쟁해결기준 대조는 미확인으로 남겼다. **검증**: `doc-lint.sh` 통과 | b4bcfee |
| 2026-09-15 | P8. `D9`·`D10` 보안·관측 | 완료 — `security-baseline.md`·`observability-rules.md` 재작성(머리말 삭제). **왜**: 선행을 23 으로 뒀었지만 근거가 이미 있었다 — 세션·CSRF·비밀번호는 3, 감사는 4, 추적은 5, 토큰은 `D12`. OWASP 2025 대응표를 이 저장소 청크로 다시 채웠다. **드러난 것**: `forward-headers-strategy` 가 미설정이다 — nginx 뒤에서 `acted_ip` 가 프록시 IP 가 된다. 33 행에 흡수했다. 관측에 **지표 이름 표 10개**를 미리 정했다 — 30 의 `MetricNamesTest` 가 이 표와 대조한다. `performance_id` 는 태그가 아니다(회차마다 시계열이 는다). **검증**: `doc-lint.sh` 통과 | 3220e31 |
| 2026-09-15 | 12f. 동시성 테스트 바탕 | 완료 — `ConcurrencyTestBase`·`ConcurrencyTestBaseTest`·`D8` 한 절. **왜**: `D8` 초안엔 `@AutoConfigureMockMvc` 가 없었는데 컨텍스트 캐시 키가 애너테이션으로 갈려서 붙였다 — 안 붙이면 컨테이너가 둘 뜬다. `D8` 스니펫을 실물에 맞췄다. **드러난 것**: 커넥션 풀 기본 10 이라 스레드 100 을 놓아도 DB 는 열 10 씩 — 재는 것은 같은 행의 두 트랜잭션이라 족하다. 풀을 키우면 속성 때문에 컨텍스트가 갈리므로 안 키운다(정보 → `D8`·바탕 KDoc). Docker Desktop 이 꺼져 있으면 컨테이너 레인이 컨텍스트 로드에서 죽는다 — 켜고 다시 돌렸다. **검증**: `integrationTest --tests ConcurrencyTestBaseTest` 2/2, `verify.sh` fast 도장 | 3b51e26 |
| 2026-09-15 | 10. 좌석 현황 조회 | 완료 — `SeatQuery`·`SeatController`·`PerformanceSeatStatus`·`Snapshot`(테스트 바탕)·`SeatQueryTest`·`SeatVersionTest`, 공개 경로 한 줄, gradle `snapshot.update` 속성. **왜**: 구역 이름 `name` 의 출처가 스키마에 없어 물었다 — **사용자가 백엔드 파생(D20 그대로)을 골랐다.** 버전은 `D20` 이 정한 대체값(`max(updated_at)` epoch 마이크로초)으로 시작한다. 재검증은 손으로 한다 — `checkNotModified` 와 `ResponseEntity` 가 `ETag` 를 둘 쓴다. **드러난 것**: `now()` 가 트랜잭션 시작 시각이라 롤백 테스트에서 버전이 안 움직인다 → 정보(`stack.md`), 그 테스트는 `ConcurrencyTestBase` 로 옮겼다. `EventFixture.hall` 에 `venueName` 을 더했다(접두 정리용). 파일 수가 상한(3)을 넘었다 — 스냅샷 바탕이 이 청크에서 처음 필요했다. **검증**: `integrationTest --tests event.* ConcurrencyTestBaseTest` 초록, `verify.sh` fast 도장 | 1f71478 |
| 2026-09-15 | 13. 좌석 선점 | 완료 — 위 분할표. **왜**: 갈리는 결정이 없었다 — `D4`·`D3`·`D5` 가 문장 순서·전이·`type` 까지 정해 뒀다. 재시도는 트랜잭션 밖(`ReservationService`)에 두고 트랜잭션은 `IdempotencyService.run` 이 연다 — 키·선점·응답이 한 트랜잭션이라야 반쪽 상태가 없다. 상한 4석은 `@Size` 가 아니라 서비스가 422 `over-limit` 으로 — `D5` 표와 맞추려고. `HoldRequest` 의 세 문장 검증(회차 상태 선읽기)은 친절한 오류용이고 강제는 조건부 UPDATE 의 `exists`. **드러난 것**: `@Transactional` 테스트 안에서는 서비스의 롤백이 안 보인다(rollback-only 표시만) → 정보(`stack.md`), 「아무것도 안 남는다」 단언은 커밋 레인으로. `EventFixture.hold` 는 예매·기록·좌석을 CTE 한 문장으로 넣는다 — 자동 커밋 바탕에서 셋을 나누면 첫 커밋에 합계 트리거가 터진다. `D4` 의 선점 경로 표기를 `D5` 대로 고쳤다. 파일 20 — 표 셋·서비스 셋·테스트 셋이 한 청크였다(분할표가 그렇게 잘랐다). 커밋 도중 perl 구분자 사고로 `D4` 가 깨진 채 커밋돼 amend 로 바로잡았다. **검증**: `test`·`integrationTest` 전부 초록(90), `verify.sh` fast 도장 | 89f79cc |
| 2026-09-15 | 14. 선점 만료 스윕 | 완료 — `HoldSweeper`·`SchedulingConfig`·`HoldSweeperTest`. **왜**: 좌석을 `performance_seat.held_until` 이 아니라 만료된 예매 id 로 고른다 — 시각으로 고르면 `paying` 예매의 좌석(여전히 `held`, 시각은 지남)까지 풀린다. 스케줄러 스위치를 기동 클래스에 안 붙이고 따로 뒀다(ProjectShop 과 같은 이유 — 프로필로 끌 자리). 테스트 컨텍스트에서도 돌지만 조건부 UPDATE 라 살아있는 선점을 안 건드린다. 지표(`seat.sweep.expired`)는 30 이 든다 — 지금은 로그 한 줄. **검증**: `verify.sh` fast 도장, `integrationTest` 전부 초록 | f159fdd |
| 2026-09-15 | 15. 1인 N매·중복 선점 제한 | 완료 — `V8`·`SeatHoldService` 수정·`ReservationLimitTest`. **왜**: 분할표대로 부분 유일 인덱스가 먼저다. 4매 합산은 인덱스로 못 걸어 앱 검증인데, **인덱스가 같은 계정의 선점을 직렬화해서** 그 검증이 경합에 안전하다 — `V8` 주석에 적었다. 중복은 예외가 아니라 `on conflict do nothing` 으로 받는다 — 예외로 받으면 트랜잭션이 어보트돼 기존 예매 id 를 못 읽는다. `on conflict` 의 상태는 리터럴이다(바인딩하면 플래너가 부분 인덱스와 못 맞춘다) — `D14` 「SQL」의 예외로 주석. **드러난 것**: 같은 계정으로 좌석 둘을 잡던 테스트 둘(`SeatQueryTest`·`HoldSweeperTest`)이 인덱스에 걸려 계정을 갈랐다. **검증**: `verify.sh` fast 도장, `integrationTest` 99 초록 | 31d7144 |
| 2026-09-15 | 16. 결제 포팅 + 확정 | 완료 — 위 분할표. **왜**: 트랜잭션 둘을 `PaymentTransitionService` 로 뺐다 — 같은 클래스 안의 자기 호출은 프록시를 안 지나 `@Transactional` 이 안 걸린다(`D14`). 멱등을 컨트롤러가 아니라 `PaymentService` 가 감싼다 — PG 호출이 트랜잭션 밖이어야 하고 그 순서를 아는 쪽이라서(ProjectShop 과 같은 판단). **무응답에 재시도하지 않는다** — `D4` 「재시도」가 이중 결제라고 했다. 대신 같은 키로 PG 에 상태를 묻고(`inquire`), 승인은 났는데 응답만 잃은 것(지연 카드 `0002`)을 그것이 살린다. `D3` 의 `requested` 결제 상태를 지웠다(문서 먼저) — PG 호출 전엔 적을 트랜잭션이 없다. 승인이 늦은 결제(`payment_late`)는 결제 행을 `approved` 로 남기고 감사 + PG 취소 — 환불 행은 17 이 만든다. **드러난 것**: 「승인이 늦었다」는 HTTP 층에서 못 만든다(`now()` 가 트랜잭션 시작 시각) → 전이 서비스 층에서 잰다. ProjectShop 결제를 옮기며 수단을 카드 하나로 줄이고(ADR 0003) 계좌이체·환불을 뺐다. **검증**: `test`·`integrationTest` 전부 초록, `verify.sh` fast 도장 | 5f647ad |
| 2026-09-15 | 17. 취소·환불 | 완료 — 위 분할표. **왜**: 판정 시각은 DB `now()`(`D7`), 산수는 순수 함수(`D8`) — 둘을 가르니 자정 경계를 빠른 레인에서 잰다. 취소 입구를 `payment` 패키지에 뒀다 — 취소가 환불을 부르고 환불이 예매를 부르므로 반대면 순환. 예매 행을 `for update` 로 잠근다 — 동시 취소 둘이 환불 행을 둘 만들려다 `refund_payment_id_key` 로 500 이 되는 것을 막는다. 멱등키는 안 받는다(`D4`) — 둘째 취소는 `invalid-transition`. **드러난 것**: PG 환불 뒤 `done` 전에 죽으면 `requested` 가 남는다 → 신규 `17b`(환불 되살리기 스윕). 커밋 레인 정리가 `payment`·`refund` 를 몰라 `restrict` 에 걸렸다 — 남은 행이 재사용 컨테이너에 쌓여 뒤 테스트 18개가 연쇄로 빨개졌다. 자식부터 지우게 고쳤다. **검증**: `test`·`integrationTest` 129 초록, `verify.sh` fast 도장 | 75866db |
| 2026-09-15 | 18. 발권 | 완료 — 위 분할표. **왜**: 발권 시점을 확정 트랜잭션 안으로 — 「확정됐는데 표가 없는 예매」가 안 남는다. 번호 충돌을 예외가 아니라 `on conflict do nothing` 의 0행으로 받는다(유일 위반은 트랜잭션을 어보트시킨다 — 15 와 같은 이유). 번호로 조회하는 경로를 안 만들었다(identifier-rules — 번호가 곧 인증이 되면 안 된다). identifier-rules 는 이식 머리말 문서라 원칙(날짜 + 난수, 32자, `SecureRandom`, 3회)만 가져오고 주문 예시는 안 믿었다 — `P11` 이 다시 쓴다. **드러난 것**: 커밋 레인 정리에 `ticket` 을 더했다. **검증**: `test`·`integrationTest` 전부 초록, `verify.sh` fast 도장 | 76f9c7b |
| 2026-09-15 | 마무리 1차 | 대상 `12f`·`10`·`13`~`18`. **대조**: `D20` 의 Redis 절반과 `/seats/changes` 를 어느 청크도 안 들고 있었다 → `10a` 신설(선행 10·21). `payment_late` 환불 행의 주인이 없었다 → `17b` ⓑ. D14 패키지 목록·backend CLAUDE.md 갱신(adde6b7). **독립 리뷰: 지적 14, 처분** — 지금 고침 13: PG 취소가 멱등 트랜잭션 안(→ 제거, 되돌리기는 환불 행 하나), 되돌리기 통로 둘(→ 17b 로 통일), `HoldSweeper` KDoc·PLAN 14 행·D4 「스윕은 held 만」이 16 뒤 낡음(→ 갱신), 취소가 `for update` 선읽기(→ 조건부 UPDATE 먼저 + 0행 진단), `UNKNOWN_LAST4=0000` 이 거절 카드와 충돌(→ 요청의 뒷 4자리), PROGRESS 표기 불일치, 결제 재전송이 `in-progress` 대신 `invalid-transition`(→ D4 에 명시), 멱등키 UUIDv4 미검사(→ 검사), 집합 대조가 `held` 뒤 한 번(→ 전이마다), 상태 코드 미저장(→ D4 에 명시). **오탐 1**: 테스트 픽스처·정리의 무조건 UPDATE — 상태를 만드는 자리라 조건부가 아니다(전이 트리거는 거친다). **드러난 것**: 당일 취소 「안 움직인다」도 롤백 레인에선 못 본다 → 커밋 레인으로(같은 `stack.md` 사실). **검증**: `verify.sh --full` 초록 두 번(adde6b7 앞·153de6b 앞) | 153de6b |
| 2026-09-15 | 19. 동시성 계측 | 완료 — 위 분할표. **왜**: `performance_seat` 에 `version` 을 안 붙이고 스크래치 표로 쟀다 — 측정 때문에 스키마를 안 바꾼다. 재는 것은 정확성이 아니라 비용이다(셋 다 승자 하나). 비관락이 4배인 근원은 잠금 대기가 커넥션을 쥐는 것 — 풀 10 앞에서 대기가 두 겹. **드러난 것**: Gradle 이 `measure` 를 up-to-date 로 건너뛰어 두 번째 실행이 첫 표를 그대로 돌려줬다 → `outputs.upToDateWhen { false }`, gradle 주석과 verify 표에 적었다. **검증**: `gradlew measure` 두 번(표 둘), `verify.sh` fast 도장 | e25674c |
| 2026-09-15 | 마무리 2차 | 대상 `19`. **독립 리뷰: 지적 3, 처분 3(지금 고침)** — 비율 `1 : 1.3 : 4.2` 가 표 어느 실행과도 안 맞았다 → 두 실행 평균 `1 : 1.4 : 4.3`, p95 `1 : 1.5 : 7.5` 로(notes·ADR 0006·D4·PLAN 행. 위 19 이력 줄의 수치는 그때의 오기라 안 고친다). 「DB 왕복 수」·「래치」 산문이 코드와 어긋남 → 잰 값이 아니라 센 값이라고 적고 래치는 바탕이 맡는다고 적음. 「전체 ms」에 스레드 생성이 든다 → 한계에 적고 p95 비율을 같이 둠. ADR 의 「여러 좌석일 때만 for update」가 `D4` 문장 순서(좌석 수 무관)와 어긋남 → 삭제. **검증**: `verify.sh --full` 초록 | 91e3ef2 |
| 2026-09-15 | P11. `stack.md`·`identifier-rules.md`·`external-references.md` 이식 | 완료 — 위 분할표. **왜**: 조건 찬 설계 청크가 이것뿐이었다(`D11` 은 선행 21). 「원칙만 믿고 예시는 안 믿는다」를 반대로 적용해 원칙이 이 저장소에서 성립하는 절만 남겼다 — 쇼핑 기능(카트·셀러·재고 `move_stock`)과 여기 없는 도구(SpotBugs·find-sec-bugs·CodeQL 팩·claude-code-action)는 지우고 `P10` 이 다시 판단하게 「아직 안 정한 것」에 뒀다. 이 세션이 밟은 사실 넷을 더했다. 식별자 규약은 「부르는 곳을 센다」로 — 예매·결제에 번호를 안 두고 404 로 숨긴다(`D5`). **드러난 것**: 바깥 링크를 다시 열지 않았다 → `external-references.md` 「확인 이력」에 적고 다음 `/inspection` 이 연다. **검증**: `StackVersionConsistencyTest` 초록, `doc-lint.sh` 통과, `verify.sh` fast 도장 | 0103da6 |
| 2026-09-15 | D11. 이벤트 카탈로그 + 알림 규약 | 완료 — `event-catalog.md` 신설. **왜**: 분할표 선행이 `21` 이었지만 근거는 대기열이 아니라 13~18 이 내는 사건이라 선행을 `18` 로 고쳤다(Fable 용량 20% 에서 최우선). 사건은 **소비자가 있는 넷만**(`reservation.reserved`·`cancelled`, `performance.closed`·`cancelled`) — `held`·`expired`·`payment.*`·`ticket.issued` 는 받는 곳이 없어 「안 내는 것」에 이유와 함께. 봉투는 자체(넷이 CloudEvents 와 1:1, 표준은 안 든다 — 소비자가 우리뿐). 페이로드는 식별자 + 그때의 값, 개인정보 없음(`D10`). 버전은 봉투에만, `type` 에 안 붙인다(파티션 키가 이름을 안 타게). 소비자 멱등은 `event_id` 유일 제약. 알림 규약을 같은 문서 한 절로 — 부르는 곳이 26 하나라서. **강제 지점**: `EventCatalogTest`(25)·스냅샷·유일 제약 — 문서라 지금은 없고 25 가 든다. **검증**: `doc-lint.sh` 통과 | b669206 |
| 2026-09-15 | I1. 점검 1차 · 세로 제약 | 완료 — 위 분할표. **판정 근거**: 이름 108 을 테스트 소스에서 이름·트리거 문구 둘로 대조했다(이름만 보면 트리거 대부분이 「안 잼」으로 나온다 — 문구로 치기 때문). 한 행 check 43 은 안 재는 것이 규칙이라 오탐. **지금 고침 4**: 트리거 `seat_grade_event_frozen`(유일하게 안 치던 것), 유일 셋(`performance_seat_key`·`reservation_seat_key`·`refund_payment_id_key`) 직접 치는 테스트. **누락 축**: 교차 표 등식 둘(결제 금액 = 예매 합계, 좌석 기록의 회차 = 예매의 회차)이 제약 없이 앱만 지킨다 → 신규 `13a`. **정보**: `D8` 에 「무엇을 치고 무엇을 안 치나」 한 줄. 지연 트리거 셋은 「걸려 있다」와 「돈다」가 같다 — 커밋 레인. **검증**: 네 클래스 초록, `verify.sh --full` 도장 | 3d50e91 |
| 2026-09-16 | 13a. 예매 교차 표 제약 | 완료 — 위 분할표. **왜**: 점검이 「앱만 지킨다」고 낸 둘을 2위로 내렸다. 지연이 아니라 즉시인 이유는 `V7` 의 합계 트리거와 다르다 — 그쪽은 예매 행이 좌석보다 먼저 들어와 커밋 때만 판정되지만, 이 둘은 삽입 순간 양쪽 행이 이미 있다. **드러난 것**: 기존 `second_approval_for_one_reservation_is_rejected` 가 금액 1 로 넣고 있어 새 트리거가 먼저 걸렸다 — 재려던 것이 `payment_approved_idx` 라 금액을 합계에 맞추고 이유를 주석에 적었다. **제약이 테스트를 가리는 자리**라 다음에 같은 모양이 또 나온다. **검증**: `test`·`integrationTest` 144 초록, `verify.sh` fast 도장 | 8cb8316 |
| 2026-09-16 | 25. 아웃박스 | 완료 — 위 분할표. **왜**: 릴레이를 깨우는 방법이 갈려 물었고 **사용자가 고정 주기 폴링(1초)** 을 골랐다 — 소비자가 메일·정산이라 1초가 문제되는 자리가 없고 코드가 한 겹 적다. `skip locked` 라 인스턴스 셋이 서로 다른 행을 밀면 그만큼 빨라진다 — 스윕과 달리 33 의 스케줄러 락이 필요 없다. `MANDATORY` 가 「사건 없이 확정 없다」의 절반을 프레임워크로 막는다. 승인이 늦은 결제(`late`)는 예매가 성립 안 했으니 사건도 안 낸다. **드러난 것**: 테스트 소스는 컴포넌트 스캔 대상이 아니라 `@Component` 만으로는 소비자 빈이 안 뜬다 → `@TestConfiguration` + `@Import`(그 `@Import` 가 컨텍스트 캐시 키를 바꿔 컨텍스트가 하나 더 뜬다 — 재사용 켜져 있어 컨테이너는 그대로). `D11` 카탈로그 표 바로 아래 「안 내는 것」 표가 같은 모양이라 파싱이 먹었다 — 끊는 자리를 문구로 고정했다. 발행된 사건의 보존 기간 청소가 없다 → 신규 `25a`. **검증**: `test`·`integrationTest` 153 초록, `verify.sh` fast 도장 | d0b54eb |
| 2026-09-16 | 16a. 판매 마감·자동 종료 | 완료 — 위 분할표. **왜**: 갈림 둘을 물었고 **사용자가 둘 다 추천안**을 골랐다 — 기본값은 `before insert` 트리거(입구가 `11`·`12` 로 늘어도 빠뜨릴 자리가 없다), 트랜잭션은 회차마다(한 회차가 계속 실패해도 남의 정산이 안 막힌다). 닫는 것은 **파는 일**이라 `reserved` 는 안 건드린다 — 무르는 것은 `17a` 고 전액 환불이 따라온다. **드러난 것 셋**: ⓐ `event` 에 두려니 `event → reservation → event` 순환이라 `ArchitectureTest` 가 잡았다 → `reservation` 패키지로(분할표가 `event/` 라 했던 자리, `RefundController` 와 같은 판단). ⓑ 픽스처의 `sales_open_at` 이 한 시간 전이라 당일 회차에서 마감과 같아져 check 위반 → 하루 전으로. ⓒ **스케줄러가 테스트에서 돌면 결과가 흔들린다** — 자동 종료가 당일 회차를 닫는 순간 그 회차의 선점이 실패한다. `ticket.scheduling.enabled` 로 테스트에서 끄고 회차를 손으로 부른다(운영 기본 `true`, `application.yml` 에 명시). 25 의 릴레이도 같은 문제를 주석으로 피하고 있었는데 같이 걷었다. **검증**: `test`·`integrationTest` 161 초록, `verify.sh` fast 도장 | db8ef50 |

## 점검 — 어디까지 봤나

`/inspection` 이 줄 하나씩 훑는다. 마지막으로 본 날이 오래된 줄이 다음이다.

| 줄 | 마지막 | 무엇을 봤나 |
|---|---|---|
| 세로 · 제약 | 2026-09-15 (`I1`) | V1~V11 제약·트리거·인덱스 108. 결과는 분할표 `I1` |
| 세로 · 테스트 | — | `D8` 층 목록에서 내려간다 |
| 세로 · 앱 검증 | — | |
| 가로 · 표준 | — | `external-references.md` 의 RFC — 링크를 다시 안 열었다(P11) |
| 가로 · 규약 | — | D 문서가 정한 것이 코드에 있나 |
| 계획 자체 | — | 청크가 겹치거나 없는 것을 가리키나 |

## 기록 규칙

- 완료했으면 「현재 상태」 표의 걸린 행을 고치고 「이력」에 한 줄 더한다. **새 줄은 맨 아래다** — 날짜순
- 「현재 상태」는 표다. 서사를 안 둔다. 25줄을 넘으면 `doc-lint.sh` 가 빨갛다
- 중간에 멈췄으면 `진행중 청크` 행에 번호와 남은 작업을 **파일명 단위**로 적는다
- 이력 한 줄은 넷을 이 차례로: **무엇을** 만들었나 → **왜** 그렇게 정했나(편 축·갈림길·사용자 선택) → **무엇이** 예보와 달랐나 → **검증** 명령. 빈 칸은 안 쓴다
- 굵게는 결정에만. 파일 이름·명령·수치는 백틱
- `PLAN.md` 완료 행은 결과(서너 줄), 이력은 서사. 같은 말을 두 벌 안 쓴다
